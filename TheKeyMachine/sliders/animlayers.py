"""Compatibility wrapper for animation-layer helpers.

The implementation lives in :mod:`TheKeyMachine.core.animlayers` so non-slider
systems can share the same layer resolution code.
"""

from TheKeyMachine.core.animlayers import *  # noqa: F401,F403
